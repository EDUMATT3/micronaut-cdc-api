datasources:
  default:
    url: jdbc:h2:mem:devDb;MVCC=TRUE;LOCK_TIMEOUT=10000;DATABASE_T0_LOWER=TRUE
    driverClassName: org.h2.Driver

#Habilita log do contexto transacional
logger:
  levels:
    io.micronaut.transaction: DEBUG